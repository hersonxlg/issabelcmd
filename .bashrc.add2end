
# vi-mode
set -o vi

# cambiar el <esc>
bind '"kj":"\e"'

# <---------------------------------------------------------------->
#                 To the end.
# <---------------------------------------------------------------->


alias sipc='asterisk -rx "sip show peers"'
alias ext='vim /etc/asterisk/extensions_custom.conf'
alias sip='asterisk -x "sip show users"'
